<style>
  :root {
    --cover-timing: 0.25s;
    --black: #000000;
  }

  .mickoy-cover {
    transition: opacity var(--cover-timing) ease-in-out;
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background-color: var(--black);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 5000;
    pointer-events: none;
    opacity: 0;
  }

  .mickoy-cover.is-active {
    opacity: 1;
  }

  .mickoy-cover__img {
    transition: transform var(--cover-timing) ease-in-out;
    width: 100%;
    max-width: 960px;
    padding: 20px;
  }

  .mickoy-cover.is-active .mickoy-cover__img {
    transform: scale(0.75) translateZ(0);
  }
</style>